import 'package:flutter/material.dart';

class Ex_SplashScreen extends StatelessWidget {
  const Ex_SplashScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}