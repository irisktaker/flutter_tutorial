import 'package:flutter/material.dart';

class Ex_PercentIndicator extends StatelessWidget {
  const Ex_PercentIndicator({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}