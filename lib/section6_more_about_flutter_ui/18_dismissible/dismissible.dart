import 'package:flutter/material.dart';

class Ex_Dismissible extends StatelessWidget {
  const Ex_Dismissible({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}