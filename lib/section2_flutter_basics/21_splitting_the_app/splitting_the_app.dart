import 'package:flutter/material.dart';

class ESplittingApp extends StatelessWidget {
  const ESplittingApp({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}