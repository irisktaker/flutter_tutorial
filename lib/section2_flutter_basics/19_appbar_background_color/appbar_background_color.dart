
import 'package:flutter/material.dart';

class EAppBarBGColor extends StatelessWidget {
  const EAppBarBGColor({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}